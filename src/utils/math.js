// Pure function
// Doesn't mutate its inputs
// Doesn't rely on global state
// Returns a value
// I can run this multiple times and I always get the same result
// Easy to test
export function add(x, y) {
  return x + y;
}
